version: "3.9"

services:
  rag-app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./vectorstore:/app/vectorstore